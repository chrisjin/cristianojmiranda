// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.unicamp.mc536.t2010s2a.forum.domain;

import br.unicamp.mc536.t2010s2a.forum.domain.TipoDocumento;
import java.util.List;
import java.util.Random;
import org.springframework.stereotype.Component;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

privileged aspect TipoDocumentoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TipoDocumentoDataOnDemand: @Component;
    
    private Random TipoDocumentoDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<TipoDocumento> TipoDocumentoDataOnDemand.data;
    
    public TipoDocumento TipoDocumentoDataOnDemand.getNewTransientTipoDocumento(int index) {
        br.unicamp.mc536.t2010s2a.forum.domain.TipoDocumento obj = new br.unicamp.mc536.t2010s2a.forum.domain.TipoDocumento();
        obj.setDsDetalhadoTipoDocumento("dsDetalhadoTipoDocumento_" + index);
        java.lang.String dsTipoDocumento = "dsTipoDocumento_" + index;
        if (dsTipoDocumento.length() > 255) {
            dsTipoDocumento  = dsTipoDocumento.substring(0, 255);
        }
        obj.setDsTipoDocumento(dsTipoDocumento);
        java.lang.String nmTipoDocumento = "nmTipoDocumento_" + index;
        if (nmTipoDocumento.length() > 50) {
            nmTipoDocumento  = nmTipoDocumento.substring(0, 50);
        }
        obj.setNmTipoDocumento(nmTipoDocumento);
        return obj;
    }
    
    public TipoDocumento TipoDocumentoDataOnDemand.getSpecificTipoDocumento(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        TipoDocumento obj = data.get(index);
        return TipoDocumento.findTipoDocumento(obj.getId());
    }
    
    public TipoDocumento TipoDocumentoDataOnDemand.getRandomTipoDocumento() {
        init();
        TipoDocumento obj = data.get(rnd.nextInt(data.size()));
        return TipoDocumento.findTipoDocumento(obj.getId());
    }
    
    public boolean TipoDocumentoDataOnDemand.modifyTipoDocumento(TipoDocumento obj) {
        return false;
    }
    
    @Transactional(propagation = Propagation.REQUIRES_NEW)
    public void TipoDocumentoDataOnDemand.init() {
        if (data != null && !data.isEmpty()) {
            return;
        }
        
        data = br.unicamp.mc536.t2010s2a.forum.domain.TipoDocumento.findTipoDocumentoEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'TipoDocumento' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<br.unicamp.mc536.t2010s2a.forum.domain.TipoDocumento>();
        for (int i = 0; i < 10; i++) {
            br.unicamp.mc536.t2010s2a.forum.domain.TipoDocumento obj = getNewTransientTipoDocumento(i);
            obj.persist();
            data.add(obj);
        }
    }
    
}
