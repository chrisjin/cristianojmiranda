// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.unicamp.mc536.t2010s2a.forum.domain;

import br.unicamp.mc536.t2010s2a.forum.domain.ProgramaDataOnDemand;
import br.unicamp.mc536.t2010s2a.forum.domain.ProgramaUsuario;
import br.unicamp.mc536.t2010s2a.forum.domain.UsuarioDataOnDemand;
import java.util.List;
import java.util.Random;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProgramaUsuarioDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ProgramaUsuarioDataOnDemand: @Component;
    
    private Random ProgramaUsuarioDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<ProgramaUsuario> ProgramaUsuarioDataOnDemand.data;
    
    @Autowired
    private ProgramaDataOnDemand ProgramaUsuarioDataOnDemand.programaDataOnDemand;
    
    @Autowired
    private UsuarioDataOnDemand ProgramaUsuarioDataOnDemand.usuarioDataOnDemand;
    
    public ProgramaUsuario ProgramaUsuarioDataOnDemand.getNewTransientProgramaUsuario(int index) {
        br.unicamp.mc536.t2010s2a.forum.domain.ProgramaUsuario obj = new br.unicamp.mc536.t2010s2a.forum.domain.ProgramaUsuario();
        obj.setIdRedeTrabalho(programaDataOnDemand.getRandomPrograma());
        obj.setIdUsuario(usuarioDataOnDemand.getRandomUsuario());
        return obj;
    }
    
    public ProgramaUsuario ProgramaUsuarioDataOnDemand.getSpecificProgramaUsuario(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        ProgramaUsuario obj = data.get(index);
        return ProgramaUsuario.findProgramaUsuario(obj.getId());
    }
    
    public ProgramaUsuario ProgramaUsuarioDataOnDemand.getRandomProgramaUsuario() {
        init();
        ProgramaUsuario obj = data.get(rnd.nextInt(data.size()));
        return ProgramaUsuario.findProgramaUsuario(obj.getId());
    }
    
    public boolean ProgramaUsuarioDataOnDemand.modifyProgramaUsuario(ProgramaUsuario obj) {
        return false;
    }
    
    @Transactional(propagation = Propagation.REQUIRES_NEW)
    public void ProgramaUsuarioDataOnDemand.init() {
        if (data != null && !data.isEmpty()) {
            return;
        }
        
        data = br.unicamp.mc536.t2010s2a.forum.domain.ProgramaUsuario.findProgramaUsuarioEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'ProgramaUsuario' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<br.unicamp.mc536.t2010s2a.forum.domain.ProgramaUsuario>();
        for (int i = 0; i < 10; i++) {
            br.unicamp.mc536.t2010s2a.forum.domain.ProgramaUsuario obj = getNewTransientProgramaUsuario(i);
            obj.persist();
            data.add(obj);
        }
    }
    
}
